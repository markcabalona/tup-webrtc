
class EnvironmentConfig {
  // List<String> get iceServerURLs {
  //   if (dotenv.env['ICE_SERVERS_URLS'] == null) {
  //     throw Exception('NO ENVIRONMENT VARIABLE FOUND');
  //   }
  //   return json.decode(dotenv.env['ICE_SERVERS_URLS']!);
  // }
}
